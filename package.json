{
  "name": "keyfall",
  "version": "0.8.0",
  "description": "Open-source piano trainer and universal MIDI keyboard controller.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node test/run.mjs",
    "dev:server": "DATA_DIR=./.data node server/mirror-server.js",
    "dev:all": "node scripts/dev-all.mjs",
    "test:setup": "npm i --no-save playwright && npm --prefix server install",
    "validate:profiles": "node scripts/validate-profiles.mjs"
  },
  "license": "MIT",
  "dependencies": {
    "@fontsource/ibm-plex-mono": "^5.3.0",
    "@fontsource/ibm-plex-sans": "^5.3.0",
    "opensheetmusicdisplay": "^1.8.7"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
