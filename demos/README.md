# Demos

Built-in demo songs live in `src/trainer/demos.js` as hard-coded note arrays for offline/no-dependency testing.

This directory is reserved for freely-licensed `.mid` files that can be shipped with the app in the future. Currently empty.
